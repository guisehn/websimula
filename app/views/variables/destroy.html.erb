<%= render 'projects/header', project: @project, internal: true %>

<div class="container">
  <h2>Aviso</h2>
  <p>Esta variável não pode ser excluída pois está sendo usada nos seguintes locais:</p>

  <%= render 'projects/usage_list', project: @project, usage: @usage %>

  <p>Remova a variável destes locais e tente novamente.</p>

  <%= link_to 'Voltar ao projeto', @project, class: 'btn btn-primary' %>
</div>
